# 🔑 Настройка SSH ключа на продакшн сервере

## Создание SSH ключа для GitHub

### 1. Проверка существующих ключей

```bash
ls -la ~/.ssh
```

Если есть файлы `id_rsa.pub` или `id_ed25519.pub`, можно использовать их. Если нет - создайте новый.

### 2. Создание нового SSH ключа

```bash
# Генерация SSH ключа (рекомендуется ed25519)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Или использовать RSA (старый метод)
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

**Интерактивные вопросы:**
- `Enter file in which to save the key`: Нажмите **Enter** (используется путь по умолчанию `~/.ssh/id_ed25519`)
- `Enter passphrase`: Нажмите **Enter** (без пароля) или введите пароль для безопасности
- `Enter same passphrase again`: Повторите пароль или нажмите **Enter**

### 3. Запуск SSH-агента

```bash
eval "$(ssh-agent -s)"
```

### 4. Добавление ключа в SSH-агент

```bash
# Для ed25519
ssh-add ~/.ssh/id_ed25519

# Для RSA
ssh-add ~/.ssh/id_rsa
```

### 5. Копирование публичного ключа

```bash
# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Или скопировать в буфер обмена (если установлен xclip)
cat ~/.ssh/id_ed25519.pub | xclip -selection clipboard
```

### 6. Добавление ключа в GitHub

1. Перейдите на https://github.com/settings/keys
2. Нажмите **"New SSH key"** или **"Add SSH key"**
3. Введите название (например, "Production Server")
4. Вставьте содержимое файла `~/.ssh/id_ed25519.pub`
5. Нажмите **"Add SSH key"**

### 7. Проверка подключения

```bash
ssh -T git@github.com
```

Вы должны увидеть:
```
Hi jonimnim! You've successfully authenticated, but GitHub does not provide shell access.
```

## Клонирование репозитория по SSH

После настройки ключа используйте SSH URL:

```bash
# Клонирование через SSH
git clone git@github.com:jonimnim/magic-ball.git

# Вместо HTTPS:
# git clone https://github.com/jonimnim/magic-ball.git
```

## Альтернативный метод: Personal Access Token (PAT)

Если SSH не подходит, используйте Personal Access Token:

### 1. Создание токена на GitHub

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Выберите права доступа: `repo` (полный доступ к репозиториям)
4. Нажмите **"Generate token"**
5. **Скопируйте токен** (он показывается только один раз!)

### 2. Использование токена при клонировании

```bash
# Клонирование с токеном
git clone https://YOUR_TOKEN@github.com/jonimnim/magic-ball.git

# Или использовать как пароль при HTTPS
git clone https://github.com/jonimnim/magic-ball.git
# Username: ваш_username
# Password: YOUR_TOKEN (вместо пароля)
```

### 3. Сохранение токена в git credentials

```bash
git config --global credential.helper store
```

При следующем клонировании git запросит логин и токен, и сохранит их.

## Обновление существующего репозитория с HTTPS на SSH

Если репозиторий уже клонирован через HTTPS:

```bash
cd magic-ball
git remote set-url origin git@github.com:jonimnim/magic-ball.git
git remote -v  # Проверка URL
```

## Безопасность

### Защита приватного ключа

```bash
# Установить правильные права доступа
chmod 600 ~/.ssh/id_ed25519
chmod 644 ~/.ssh/id_ed25519.pub
```

### Использование passphrase (рекомендуется)

При создании ключа используйте пароль для дополнительной безопасности.

### Ограничение доступа ключа

В настройках GitHub можно ограничить доступ ключа только к нужным репозиториям (если используется Deploy Key).

## Быстрая команда (одной строкой)

```bash
ssh-keygen -t ed25519 -C "prod-server" -f ~/.ssh/id_ed25519 -N "" && cat ~/.ssh/id_ed25519.pub
```

Создаст ключ без пароля и сразу покажет публичный ключ для копирования в GitHub.

