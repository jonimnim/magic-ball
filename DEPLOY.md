# 🚀 Развертывание на продакшн сервере

## Быстрый старт

### 0. Настройка SSH ключа (если нужно)

Если планируете клонировать через SSH, сначала настройте SSH ключ:
```bash
# Создание SSH ключа
ssh-keygen -t ed25519 -C "prod-server"

# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub
```

Скопируйте ключ и добавьте в GitHub: https://github.com/settings/keys

Подробные инструкции в [SSH_SETUP.md](SSH_SETUP.md)

### 1. Клонирование репозитория

```bash
# Через SSH (после настройки ключа)
git clone git@github.com:jonimnim/magic-ball.git

# Или через HTTPS
git clone https://github.com/jonimnim/magic-ball.git

cd magic-ball
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
cat > .env << EOF
DEEPSEEK_API_KEY=your_deepseek_api_key_here
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
EOF
```

Или скопируйте файл с другого сервера:

```bash
scp user@source-server:/path/to/.env .
```

### 3. Установка Docker и Docker Compose

Если Docker не установлен:

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
sudo apt-get install docker-compose-plugin -y

# Выйдите и зайдите заново для применения прав группы
```

### 4. Запуск проекта

#### Запуск только Telegram бота (рекомендуется для продакшн)

```bash
docker-compose up -d telegram-bot
```

#### Запуск всего стека

```bash
docker-compose up -d
```

#### Проверка статуса

```bash
docker-compose ps
```

#### Просмотр логов

```bash
# Логи Telegram бота
docker-compose logs -f telegram-bot

# Логи консольной версии
docker-compose logs -f magic-ball

# Все логи
docker-compose logs -f
```

### 5. Обновление проекта

```bash
# Остановить контейнеры
docker-compose down

# Получить последние изменения
git pull

# Пересобрать и запустить
docker-compose up -d --build
```

## Автоматическое развертывание

Используйте скрипт `deploy.sh` для автоматического развертывания:

```bash
chmod +x deploy.sh
./deploy.sh
```

## Ручное развертывание

### Шаг 1: Остановка текущих контейнеров (если есть)

```bash
docker-compose down
```

### Шаг 2: Получение обновлений

```bash
git pull origin main
```

### Шаг 3: Пересборка образов

```bash
docker-compose build
```

### Шаг 4: Запуск

```bash
docker-compose up -d
```

### Шаг 5: Очистка старых образов (опционально)

```bash
docker system prune -f
```

## Мониторинг

### Проверка работы контейнеров

```bash
docker ps
```

### Статистика использования ресурсов

```bash
docker stats
```

### Перезапуск сервиса

```bash
docker-compose restart telegram-bot
```

## Проблемы и решение

### Контейнер не запускается

```bash
# Проверьте логи
docker-compose logs telegram-bot

# Проверьте конфигурацию
docker-compose config
```

### Обновление переменных окружения

После изменения `.env` файла:

```bash
docker-compose down
docker-compose up -d
```

### Проблемы с правами

```bash
sudo chown -R $USER:$USER .
```

